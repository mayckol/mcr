[package]
name = "mcr-core"
version = "0.1.0"
edition = "2021"
description = "Three-way merge, diff, alignment, and reversible apply/revert engine for MCR"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
similar = "2"

[dev-dependencies]
proptest = "1"
